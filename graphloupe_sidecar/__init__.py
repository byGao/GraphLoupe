"""GraphLoupe sidecar package (foundation scaffold).

Holds the minimal real graphs that back the L0 PIN tests (test-plan.html) and
pin_dump.py. The Extension / FastAPI sidecar / WebSocket layer arrives in PHASE 1.
"""
