{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run GraphLoupe Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/*.js"],
      "preLaunchTask": "graphloupe-build"
    }
  ]
}
